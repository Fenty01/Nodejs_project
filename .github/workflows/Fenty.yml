name: Node CI
on: push
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/setup-node@v6.3.0
      - name: Install dependencies
        run: npm install
      - name: Run tests
        run: npm test
